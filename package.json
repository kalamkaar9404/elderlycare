{
  "name": "mednutri-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy":  "npx hardhat run scripts/deploy.mjs --network amoy",
    "anchor":  "npx hardhat run scripts/anchorMockPatient.mjs --network amoy"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "ethers": "^6.0.0",
    "hardhat": "^3.1.11"
  }
}
